import os
import asyncio
from aiogram import Bot, Dispatcher, Router, F
from aiogram.types import Message, Update
from fastapi import FastAPI, Request
import uvicorn
import requests
import json

BOT_TOKEN = os.getenv("BOT_TOKEN")
REPLICATE_API_KEY = os.getenv("REPLICATE_API_TOKEN")
ADMIN_ID = int(os.getenv("ADMIN_TELEGRAM_ID", "0"))

bot = Bot(token=BOT_TOKEN)
dp = Dispatcher()
router = Router()

SYSTEM_PROMPT = """Ты — помощник детской IT-школы Технодром. Общаешься с родителями в Telegram. Твоя задача — помогать записываться на занятия, отвечать на вопросы и создавать ощущение тёплого, заботливого места для детей.

❗ ВАЖНО:
— Никогда не говори, что ты бот или ИИ. Ты — часть команды Технодром.
— Не используй фразу «в технодроме». Вместо этого говори: «у нас», «в нашей школе», «в Технодроме» (название не склоняется!).
— Мы принимаем детей от 4 лет.
— У нас два типа абонементов: разовый (1 занятие) и мини (4 занятия). Неиспользованные занятия можно перенести при условии уведомления за 24 часа.
— Всегда упоминай: у нас есть печеньки и уютная атмосфера.
— Предлагай бесплатное пробное занятие в любом направлении — робототехника, программирование, Minecraft, 3D-моделирование.
— При записи уточняй: имя ребёнка, возраст, удобный день и время.
— Если спрашивают про состав групп — говори: «Мы работаем в мини-группах до 6 человек, каждый ребёнок двигается в своём темпе».
— Если спрашивают про конструкторы — мягко объясни: «У нас ограниченное количество наборов, поэтому мы работаем с общим пулом деталей — это учит детей договариваться и помогать друг другу».
— Запоминай имена детей и родителей в диалоге.
— Если вопрос сложный (жалоба, конфликт, особые условия) — не обещай решить сам. Скажи: «Давайте я уточню у администратора и сразу вернусь».

📍 Адрес:
ул. Суворова, д. 1/9, 1 этаж, отдельный вход слева от торгового центра.

📞 Контакт:
Администратор Технодром: +7 (960) 446-48-44

🕒 Часы работы:
Пн–Вс: 10:00–20:00 (без выходных)

🌐 Сайт: https://techdrom.com

💡 Фразы для вдохновения:
— «Мы верим: никогда не поздно сделать первый шаг»
— «У нас каждый ребёнок — первооткрыватель»

🚫 Что НЕ делать:
— Не обещай индивидуальный график «навсегда».
— Не называй детей «медленными» — говори «идут в своём темпе».
— Не используй слово «потерянные» — только «неиспользованные» (для занятий).
— Не оправдывайся за ограничения — мягко объясняй логику.
— Не предлагай написать на почту — у нас нет email.

Тон общения: тёплый, заботливый, как сотрудник, который любит своё дело. Короткие предложения. Без эмодзи или максимум 1–2 в начале диалога."""

user_sessions = {}

async def ask_llm(user_id, text):
    if user_id not in user_sessions:
        user_sessions[user_id] = []
    
    history = ""
    for msg in user_sessions[user_id][-6:]:
        role = "Пользователь" if msg["role"] == "user" else "Ты"
        history += f"{role}: {msg['content']}\n"
    
    prompt = f"{SYSTEM_PROMPT}\n\nИстория диалога:\n{history}\nПользователь: {text}\nТы:"
    
    try:
        response = requests.post(
            "https://api.replicate.com/v1/predictions",
            headers={
                "Authorization": f"Token {REPLICATE_API_KEY}",
                "Content-Type": "application/json"
            },
            json={
                "version": "8f19256b502806e9d8c390c02d379f42c2c6109e5d8d421c8a344f4859b8de40",
                "input": {
                    "prompt": prompt,
                    "max_tokens": 250,
                    "temperature": 0.7,
                    "top_p": 0.9
                }
            },
            timeout=60
        )
        
        if response.status_code != 201:
            print(f"Replicate error {response.status_code}: {response.text}")
            return "Секунду, уточняю детали..."
        
        prediction_id = response.json()["id"]
        
        for _ in range(30):
            await asyncio.sleep(2)
            result_resp = requests.get(
                f"https://api.replicate.com/v1/predictions/{prediction_id}",
                headers={"Authorization": f"Token {REPLICATE_API_KEY}"}
            )
            
            if result_resp.status_code == 200:
                result = result_resp.json()
                if result["status"] == "succeeded":
                    answer = result["output"][0].strip()
                    if "уточню у администратора" in answer.lower() or "спрошу у администратора" in answer.lower():
                        await bot.send_message(
                            chat_id=ADMIN_ID,
                            text=f"⚠️ Нужна помощь!\n\nКлиент ID: {user_id}\n\nСообщение: {text}\n\nБот ответил: {answer}"
                        )
                    user_sessions[user_id].append({"role": "user", "content": text})
                    user_sessions[user_id].append({"role": "assistant", "content": answer})
                    return answer
                elif result["status"] in ["failed", "canceled"]:
                    return "Секунду, уточняю детали..."
        
        return "Секунду, уточняю детали..."
            
    except Exception as e:
        print(f"Ошибка: {e}")
        return "Секунду, уточняю детали..."

@router.message(F.text)
async def handle_message(message: Message):
    answer = await ask_llm(message.from_user.id, message.text)
    await message.answer(answer)

dp.include_router(router)

app = FastAPI()

@app.on_event("startup")
async def on_startup():
    base_url = os.getenv('RENDER_EXTERNAL_URL', 'https://techdrom-bot.onrender.com')
    webhook_url = f"{base_url}/webhook"
    await bot.set_webhook(url=webhook_url)

@app.post("/webhook")
async def webhook(request: Request):
    update_data = await request.json()
    update = Update(**update_data)
    await dp.feed_update(bot, update)
    return {"ok": True}

@app.get("/")
async def health_check():
    return {"status": "ok"}

if __name__ == "__main__":
    port = int(os.getenv("PORT", "8000"))
    uvicorn.run(app, host="0.0.0.0", port=port)
